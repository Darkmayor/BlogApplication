spring:
  mail:
    transport:
      protocol: smtp
    smtp:
      ssl:
        trust: smtp.gmail.com  
    username: pubgtest01984@gmail.com
    password: 
    host: smtp.gmail.com
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  datasource:
    url: jdbc:mysql://localhost:3306/restraurant
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect:org.hibernate.dialect.MySQL8Dialect
thymeleaf-layout-dialect:
  .version=2.1.1

